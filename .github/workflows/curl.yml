name: github curl
on: push

jobs:
  curl:
    name: github curl
    runs-on: ubuntu-latest
    steps:
      - run: curl -i -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" https://api.github.com/user/repos
